INSTALLATION NOTES
==================

cd /some/dir
tar -xvzf /somewhere/lower_layer.tgz
cd ll/lib
gnatmake lower_layer_udp
gnatmake lower_layer_tcp

Add /some/dir/ll/lib to both ADA_INCLUDE_PATH and ADA_OBJECTS_PATH, 
or use gnatmake with -I/some/dir/ll/lib.

